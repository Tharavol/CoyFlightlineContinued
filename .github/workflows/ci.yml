name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  # Cheap and toolchain-free, so it runs alongside the lint job rather than
  # behind it: a broken TOC is worth hearing about even if luacheck is red.
  toc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Validate the TOC file list and load order
        run: sh .github/scripts/validate-toc.sh

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: leafo/gh-actions-lua@v13
        with:
          luaVersion: "5.1"

      - uses: leafo/gh-actions-luarocks@v6

      - name: Install luacheck
        run: luarocks install luacheck

      - name: Luacheck
        run: luacheck .

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: leafo/gh-actions-lua@v13
        with:
          luaVersion: "5.1"

      - uses: leafo/gh-actions-luarocks@v6

      - name: Install busted
        run: luarocks install busted

      - name: Busted
        run: busted

  package:
    needs: [toc, lint, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      # If this push also carries a tag on HEAD (e.g. a release commit pushed
      # together with its tag), BigWigsMods/packager intentionally no-ops on
      # the branch push to avoid a duplicate build, leaving no zip behind.
      # Detect that ourselves so the later steps skip deterministically
      # instead of racing the tag ref and failing when they lose.
      - name: Check for pending tag
        id: check_tag
        run: echo "tag=$(git tag --points-at HEAD)" >> "$GITHUB_OUTPUT"

      # -d skips uploading, so this builds the real release zip on every push
      # without publishing it. Packaging breakage surfaces here rather than
      # at tag time.
      - name: Package (dry run)
        if: steps.check_tag.outputs.tag == ''
        uses: BigWigsMods/packager@v2
        with:
          args: -d

      - name: Verify archive layout
        if: steps.check_tag.outputs.tag == ''
        run: |
          zip=$(ls .release/*.zip)
          unzip -l "$zip"
          unzip -l "$zip" | grep -q 'CoyFlightline/CoyFlightline.toc'
          unzip -l "$zip" | grep -q 'CoyFlightline/Core.lua'
          unzip -l "$zip" | grep -q 'CoyFlightline/Config.lua'
          unzip -l "$zip" | grep -q 'CoyFlightline/Minimap.lua'
          unzip -l "$zip" | grep -q 'CoyFlightline/WorldMap.lua'
          unzip -l "$zip" | grep -q 'CoyFlightline/Settings.lua'

          # Dev-only tooling / docs; .pkgmeta ignores them, and they should stay ignored.
          # A bare `!` here would be exempt from set -e and silently not fail.
          if unzip -l "$zip" | grep -q 'CoyFlightline/spec/'; then
            echo "spec/ should not ship in the release archive (see .pkgmeta)" >&2
            exit 1
          fi
          if unzip -l "$zip" | grep -q 'CoyFlightline/CHANGELOG.md'; then
            echo "CHANGELOG.md should not ship in the release archive (see .pkgmeta)" >&2
            exit 1
          fi
